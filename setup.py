from setuptools import find_packages, setup

setup(
    name="visibility-prediction-system",
    version="1.0.0",
    description="Aviation Visibility Prediction System",
    packages=find_packages(),
    install_requires=[],
)
